 private static final Pattern PASSWORD_PATTERN = Pattern.compile("(?i)\b(?:\w+\s+)?(?:password|pwd)\s*[:=]\s*([^\s]+(?:\s+[^\s]+)*)\b", Pattern.CASE_INSENSITIVE);
    private static final String MASK = "$1*****";
